/**
 * Hack that, if you enable it in Settings, uses the system color "Window" instead of "white" as the background
 * color of text fields, text areas and similar controls.
 */
package cz.hudecekpetr.snowride.fx.systemcolor;